Code can be compiled using following command:
g++ vectorGraph.cpp –fopenmp

The executable so compiled namely a.out can be run as:

./a.out outfile_1000_50.txt

Input to program is name of input file with graph information.

Format of Input File expected:
[Total number of nodes]
edge_node1 edge_node2 edge_cost
.................................
.................................
.................................
.................................

Attached are some of the generated random graphs to test the algorithm.
